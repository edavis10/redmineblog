---
---
<h1>
  <%= tag.capitalize %> Articles
</h1>

<div class="tag-page">
  <% items_with_tag(tag).each do |item| %>
    <%= render('_article', :item => item, :is_embedded => true) %>
  <% end %>
</div>
